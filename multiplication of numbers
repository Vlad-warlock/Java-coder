public class Main {
    public static void main(String[] args) {
        System.out.println(multiply(5, 8));
    }

    /*
     * метод возвращает произведение(умножение) двух чисел
     */
    static long multiply(int n, int m) {
        return n * m;
    }
}
